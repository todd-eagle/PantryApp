import React from 'react'
import { View, Text } from 'react-native'

const ProdHeaderTitle = ({title}) => {
    return (
        <View>
            <Text>{title}</Text>
        </View>
    )
}

export default ProdHeaderTitle
